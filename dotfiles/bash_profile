# shellcheck shell=bash
# ~/.bash_profile — login shells.
#
# bash reads this instead of ~/.profile. ~/.bashrc owns the shared PATH logic
# because sshd also reads it for non-interactive remote commands; sourcing it
# here gives login and interactive shells the identical result.
[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
