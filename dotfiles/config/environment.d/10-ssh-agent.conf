# One OpenSSH agent socket for GNOME Wayland terminals and headless sessions.
# Forwarded agents are preserved by the interactive-shell guard in ~/.bashrc.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/openssh_agent
