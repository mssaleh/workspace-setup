# Apple Container policy shared by ordinary and amd64 workloads. CPU and memory
# are deliberately absent: Apple's defaults remain in force until the user
# explicitly runs the apple-container-amd64 optimizer for a heavy build.
[build]
rosetta = true

[registry]
domain = "docker.io"
